spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/snsproject?characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: 1111

mybatis:
  mapper-locations: classpath:mapper/*.xml

